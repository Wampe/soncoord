<Window x:Class="Soncoord.Client.WPF.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:infrastructure="clr-namespace:Soncoord.Infrastructure;assembly=Soncoord.Infrastructure"
        mc:Ignorable="d"
        Title="Soncoord" Height="720" Width="1280" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock VerticalAlignment="Center" Margin="16">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}{0:00}:{1:00;00}">
                    <Binding Path="PlayerTime.Minutes"/>
                    <Binding Path="PlayerTime.Seconds"/>
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
        <ContentControl prism:RegionManager.RegionName="{x:Static infrastructure:Regions.ShellContent}" Grid.Row="1"/>
    </Grid>
</Window>
